# 象棋线上对战服务器 Heroku Procfile
# 
# 定义 Heroku 应用的进程类型

# Web 进程
web: node server/dist/server.js

# 可选：Worker 进程（用于后台任务）
# worker: node server/dist/worker.js

# 可选：Scheduler 进程（用于定时任务）
# scheduler: node server/dist/scheduler.js

